# 🚀 Запуск проекта с Claude Code

## Шаг 1: Установка (если ещё не сделал)

```bash
# Проверь Node.js
node --version   # Нужен 18+

# Установи Claude Code
npm install -g @anthropic-ai/claude-code
```

## Шаг 2: Запуск

```bash
cd C:\Projects\FrtOffersCopilot
claude
```

## Шаг 3: Первая команда

Скопируй и вставь в Claude Code:

```
Прочитай TASK.md и все файлы в /docs и /data папках.

Твоя задача - создать MVP генератора Firm Offers.

Начни с:
1. Изучи структуру данных в /data (ports.json, cargo_stw.json, charterers.json)
2. Создай backend на FastAPI с endpoint POST /api/generate
3. Создай простой frontend с формой

Используй данные из JSON файлов, не выдумывай свои.
Начинай с backend, потом frontend.
```

## Что должно получиться

После работы Claude Code у тебя будет:

```
FrtOffersCopilot/
├── docs/              ✓ Готово
├── data/              ✓ Готово  
├── examples/          ✓ Готово
├── backend/           ← Claude Code создаст
│   ├── app/
│   │   ├── main.py
│   │   ├── generator.py
│   │   └── models.py
│   └── requirements.txt
├── frontend/          ← Claude Code создаст
│   ├── index.html
│   └── ...
├── TASK.md            ✓ Готово
└── README.md          ✓ Готово
```

## Команды для проверки

После создания:

```bash
# Запуск backend
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload

# Открой в браузере
http://localhost:8000/docs   # Swagger UI
```

## Если что-то не работает

Скажи Claude Code:
```
Покажи ошибку и исправь её
```

или

```
Запусти backend и покажи что не так
```
